apiVersion: image.openshift.io/v1
kind: ImageStream
metadata:
  name: ncms-frontend
  namespace: ncms              # ← change to your OCP namespace/project
  labels:
    app: ncms-frontend
spec:
  lookupPolicy:
    local: true
